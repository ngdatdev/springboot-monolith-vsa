spring:
  application:
    name: java-vsa-monolith-sourcebase
    admin:
      enabled: false
  output:
    ansi:
      enabled: ALWAYS
  
  # Database Configuration (Advanced)
  datasource:
    url: jdbc:postgresql://localhost:5000/commerce_core
    username: commerce_core
    password: zxcl123123
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 10
      idle-timeout: 300000
      connection-timeout: 30000
      max-lifetime: 1800000
      pool-name: VSA-Ecommerce-Hikari-Pool
    jpa:
      ddl-auto: update
      show-sql: false
      format-sql: true
  
  # Spring standard jpa section is now restricted to non-datasource specific settings
  jpa:
    open-in-view: false

  # Jackson Configuration
  jackson:
    serialization:
      fail-on-empty-beans: false

  # Security Configuration
  # Security Configuration
security:
  jwt:
    # IMPORTANT: Change this in production! Store in environment variable
    # Must be at least 256 bits (32 bytes) for HS256
    # Generate with: openssl rand -base64 32
    # Secret key for HS512 (must be >= 512 bits / 64 bytes)
    secret-key: 94dVTstqm+0ZlG5DHL9WJBIs4LFxTmuoDi/3YQQPgJx4IVW1HFHhrvAi1pm7m1wHp1R7VQL+HuRiopkoezz5yNg==
    expiration-ms: 3600000       # 1 hour (in milliseconds)
    refresh-expiration-ms: 86400000  # 24 hours (in milliseconds)
    issuer: vsa-ecommerce

# Snowflake ID Generator Configuration
snowflake:
  datacenter-id: 21  # 0-31
  worker-id: 21         # 0-31


mail:
  host: smtp.gmail.com
  port: 587
  username: nguyen.dat.fr@gmail.com
  password: zona mayl ivjz udyr
  from: nguyen.dat.fr@gmail.com
  properties:
    mail:
      smtp:
        auth: true
        starttls:
          enable: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

# Cloudinary Configuration
cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME:your_cloud_name}
  api-key: ${CLOUDINARY_API_KEY:your_api_key}
  api-secret: ${CLOUDINARY_API_SECRET:your_api_secret}
  default-folder: vsa-ecommerce

logging:
  level:
    root: INFO
    org.springdoc: DEBUG
    org.springframework.web: DEBUG
    com.vsa.ecommerce: DEBUG
    com.vsa.monolith: DEBUG
    org.springframework.web.servlet.resource.ResourceHttpRequestHandler: INFO
    org.springframework.web.servlet.mvc.method.annotation.ExceptionHandlerExceptionResolver: DEBUG
    
server:
  port: 8081

management:
  health:
    mail:
      enabled: false
    redis:
      enabled: false

# Custom Cache Configuration
cache:
  default-namespace: vsa
  default-version: v1
  caffeine:
    max-size: 1000
    ttl-seconds: 30
  redis:
    ttl-minutes: 5
  pub-sub:
    channel: cache-invalidation
  locks:
    wait-time-seconds: 10
    lease-time-seconds: 30

# Background Jobs Configuration
jobs:
  enabled: true  # Master switch for all background jobs

redis:
  host: valkey-3279477f-ngdat.h.aivencloud.com
  port: 28168
  username: default
  password: AVNS_fOw0EaiZySOfre8OqIZ
  database: 0
  ssl:
    enabled: true
  timeout: 5s
  lettuce:
    pool:
      max-active: 8
      max-idle: 8
      min-idle: 2
      max-wait: -1ms

# SpringDoc OpenAPI Configuration
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    disable-swagger-default-url: true # Prevent default petstore
    operations-sorter: method
  packages-to-scan: com.vsa.ecommerce